---

# Pre-install 
# TODO: validate the different identity provider kinds as well
- name: Pre-install 
  hosts: all
  roles:
     - hostname
     - package
- name: Pre-install 
  hosts: all
  roles:
     - master
